import ClipSpan from "../../../ClipSpan"

const StartIcon = ({ label }) => (
  <ClipSpan className="!py-0 font-urwgeometric_bold text-[18px] !leading-[100%]">{label}</ClipSpan>
)

export default StartIcon
